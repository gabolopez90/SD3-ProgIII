import { HighlightChange } from './highlight-change';

describe('HighlightChange', () => {
  it('should create an instance', () => {
    const directive = new HighlightChange();
    expect(directive).toBeTruthy();
  });
});
